\documentclass{article}
\begin{document}
Rerooting changed at least 2.1\% of tree edges. %PROV[same1]
Re-rendered, the share is still 2.1\%. %PROV[same2] %PROV[changed]
Here 50\% of sites vary, which is not a tag.
A glued sentence.%PROV[glued]
The config was written with CRLF line endings. %PROV[crlf]
%PROV free text, not a tag
\input{sections/methods}
\end{document}
